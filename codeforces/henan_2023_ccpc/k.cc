#include <bits/stdc++.h>

int main() {
    string s;
    while (cin >> s) {
        if (s == "Is it rated?") {
            cout << "NO\n";
        } else {
            cout << "YES\n";
        }
    }
}